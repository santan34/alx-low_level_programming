#include <stdio.h>
/**
 * main - Entry point  
 * code to decide if a random number is even or odd 
 *  Return: Always 0 (Success) 
 */

int main() 
{
   int ch;
for (ch = 97; ch<=122; ch++)
   {
!(ch==101 || ch  == 113) &&putchar(ch);
   };
    return (0);
}

